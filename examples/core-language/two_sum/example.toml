description = "Two-sum via a hash map"
source   = "leetcode two_sum"
category = "core-language"
showcase = false
forces   = []   # TODO backfill against lang-spec artefact IDs
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "two_sum.td"

[[error]]
patch   = "errors/typo-want.patch"   # typo: wnat is not in scope
expect  = ["E0103"]
stage   = "sema"
matches = "errors/typo-want.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/missing-in-for.patch"   # for-header missing the in keyword
expect  = ["E0112"]
stage   = "parse"
matches = "errors/missing-in-for.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/missing-comma-args.patch"   # missing comma between call arguments
expect  = ["E0112"]
stage   = "parse"
matches = "errors/missing-comma-args.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/organic-twosum2-e0201.patch"   # Two-pass hash-map approach: store seen numbers by value, check if complement …
expect  = ["E0201"]
stage   = "sema"
matches = "errors/organic-twosum2-e0201.expected"
origin  = "organic:holefill-2"

[[error]]
patch   = "errors/organic-twosum2-e0203.patch"   # Use a hash map to track indices of seen values; for each number, check if its …
expect  = ["E0203"]
stage   = "sema"
matches = "errors/organic-twosum2-e0203.expected"
origin  = "organic:holefill-2"
