description = "JSON REST CRUD mapping Result to HTTP status"
source   = "services todo_api"
category = "stdlib-binding"
showcase = false
forces   = []   # TODO backfill against lang-spec artefact IDs
since    = "0.1"
status   = "target-sketch"
expects  = "no-run"
entry    = "todo_api.td"
