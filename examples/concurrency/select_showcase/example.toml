description = "Every select case form"
source   = "concurrency select_showcase"
category = "concurrency"
showcase = false
forces   = []   # TODO backfill against lang-spec artefact IDs
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "select_showcase.td"

[[error]]
patch   = "errors/annot-mismatch.patch"   # n annotated string but assigned an int
expect  = ["E0201"]
stage   = "sema"
matches = "errors/annot-mismatch.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/stray-break.patch"   # a stray break with no enclosing loop
expect  = ["E0404"]
stage   = "sema"
matches = "errors/stray-break.expected"
origin  = "synthetic"
