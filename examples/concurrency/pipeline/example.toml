description = "Three-stage channel pipeline"
source   = "concurrency pipeline"
category = "concurrency"
showcase = false
forces   = []   # TODO backfill against lang-spec artefact IDs
since    = "0.1"
status   = "running"
expects  = "run-pass"
entry    = "pipeline.td"

[[error]]
patch   = "errors/scope-string-error.patch"   # scope error parameter must be error in v1
expect  = ["E0407"]
stage   = "sema"
matches = "errors/scope-string-error.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/spawn-outside-scope.patch"   # spawn used outside a scope block
expect  = ["E0405"]
stage   = "sema"
matches = "errors/spawn-outside-scope.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/annot-mismatch.patch"   # total annotated string but assigned an int
expect  = ["E0201"]
stage   = "sema"
matches = "errors/annot-mismatch.expected"
origin  = "synthetic"

[[error]]
patch   = "errors/dup-produce.patch"   # a second top-level declaration of produce
expect  = ["E0113"]
stage   = "sema"
matches = "errors/dup-produce.expected"
origin  = "synthetic"
